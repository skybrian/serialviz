{
  "scripts": {
    "build": "esbuild app.ts --bundle --sourcemap --outfile=site/app.js",
    "check": "tsc --noEmit",
    "launch": "open -a 'Google Chrome' site/index.html"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "typescript": "^4.6.3",
    "@types/w3c-web-serial": "^1.0.2"
  }
}
