{
  "scripts": {
    "build": "esbuild app.ts app.css --bundle --sourcemap --outdir=site",
    "check": "tsc --noEmit",
    "dev": "npm run build -- --servedir=site",
    "chrome": "open -a 'Google Chrome' http://127.0.0.1:8000/)"
  },
  "devDependencies": {
    "@types/w3c-web-serial": "^1.0.2",
    "esbuild": "^0.14.38",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "xterm": "^4.18.0"
  }
}
